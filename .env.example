# Copy to .env — all values can also be set later in the Web GUI (Settings tab).
HEVY_API_KEY=
WGER_BASE_URL=
WGER_API_KEY=
# 5-field crontab (min hour dom mon dow); default hourly at :00
SYNC_CRON=0 * * * *
